/*Escribimos una función llamada ealax que calcula aproximada el valor de e^x*/

#include<stdio.h>

double ealax(double x, int n);
int factorial(int x);
int pot(int x, int y);

int main(void){
}

double ealax(double x, int n){
	int i;
	factorial(i);
	pot
	total = 0;
	for(i=1; i < n; i++){
		total += 1 + 
	}
}

int factorial(int x){
	int total = 1;
	int i;
	int j = 1;
	for(i = 0; i < x ; i++){
		total *= j * (j + i);
	}
	return factorial;
}

int pot(int x, int y){
	int i;
	int total = 1;
	for(i = 0; i < y; i++){
		total *= x;
	}
	return total;
}
